const sqlQuestions = [
    // BEGINNER LEVEL (1-40)
    { id: 1, difficulty: "beginner", question: "What does SQL stand for?", options: ["Structured Query Language", "Simple Question Language", "Standard Query Language", "System Query Language"], correct: 0, example: "SQL is used to communicate with databases:\nSELECT * FROM employees;", explanation: "SQL stands for Structured Query Language. It is a standard language for storing, manipulating, and retrieving data in databases." },
    { id: 2, difficulty: "beginner", question: "Which SQL statement is used to extract data from a database?", options: ["GET", "EXTRACT", "SELECT", "OPEN"], correct: 2, example: "SELECT column1, column2\nFROM table_name;", explanation: "The SELECT statement is used to select data from a database. The data returned is stored in a result table." },
    { id: 3, difficulty: "beginner", question: "Which SQL statement is used to update data in a database?", options: ["MODIFY", "UPDATE", "SAVE", "CHANGE"], correct: 1, example: "UPDATE employees\nSET salary = 50000\nWHERE emp_id = 101;", explanation: "The UPDATE statement is used to modify existing records in a table. Always use WHERE clause to specify which records to update." },
    { id: 4, difficulty: "beginner", question: "Which SQL statement is used to delete data from a database?", options: ["REMOVE", "DELETE", "DROP", "ERASE"], correct: 1, example: "DELETE FROM employees\nWHERE emp_id = 101;", explanation: "The DELETE statement is used to delete existing records in a table. Without WHERE clause, all records will be deleted!" },
    { id: 5, difficulty: "beginner", question: "Which SQL statement is used to insert new data in a database?", options: ["ADD", "INSERT INTO", "CREATE", "PUT"], correct: 1, example: "INSERT INTO employees (name, salary)\nVALUES ('John Doe', 45000);", explanation: "The INSERT INTO statement is used to insert new records in a table." },
    { id: 6, difficulty: "beginner", question: "How do you select a column named 'FirstName' from a table named 'Persons'?", options: ["SELECT Persons.FirstName", "EXTRACT FirstName FROM Persons", "SELECT FirstName FROM Persons", "GET FirstName FROM Persons"], correct: 2, example: "SELECT FirstName FROM Persons;", explanation: "The basic syntax is SELECT column_name FROM table_name." },
    { id: 7, difficulty: "beginner", question: "How do you select all columns from a table named 'Persons'?", options: ["SELECT *.Persons", "SELECT * FROM Persons", "SELECT [all] FROM Persons", "SELECT Persons"], correct: 1, example: "SELECT * FROM Persons;", explanation: "The asterisk (*) is a wildcard that selects all columns from the specified table." },
    { id: 8, difficulty: "beginner", question: "How can you return records from 'Persons' sorted descending by 'FirstName'?", options: ["SELECT * FROM Persons SORT BY FirstName DESC", "SELECT * FROM Persons ORDER FirstName DESC", "SELECT * FROM Persons ORDER BY FirstName DESC", "SELECT * FROM Persons SORT 'FirstName' DESC"], correct: 2, example: "SELECT * FROM Persons\nORDER BY FirstName DESC;", explanation: "ORDER BY is used to sort the result-set. DESC means descending order, ASC means ascending (default)." },
    { id: 9, difficulty: "beginner", question: "How can you return the number of records in the 'Persons' table?", options: ["SELECT COUNT(*) FROM Persons", "SELECT NUM(*) FROM Persons", "SELECT COLUMNS(*) FROM Persons", "SELECT LEN(*) FROM Persons"], correct: 0, example: "SELECT COUNT(*) FROM Persons;", explanation: "COUNT(*) returns the number of rows in a table." },
    { id: 10, difficulty: "beginner", question: "Which SQL keyword is used to retrieve only unique values?", options: ["UNIQUE", "DIFFERENT", "DISTINCT", "SEPARATE"], correct: 2, example: "SELECT DISTINCT country\nFROM customers;", explanation: "DISTINCT is used to return only different (distinct) values, eliminating duplicates." },
    { id: 11, difficulty: "beginner", question: "Which SQL keyword is used to filter records?", options: ["FILTER", "WHERE", "HAVING", "IF"], correct: 1, example: "SELECT * FROM employees\nWHERE salary > 50000;", explanation: "WHERE clause is used to filter records based on specified conditions." },
    { id: 12, difficulty: "beginner", question: "Which operator is used to search for a specified pattern in a column?", options: ["FIND", "LIKE", "SEARCH", "MATCH"], correct: 1, example: "SELECT * FROM customers\nWHERE name LIKE 'J%';", explanation: "LIKE operator is used in WHERE clause to search for a specified pattern. % represents zero or more characters." },
    { id: 13, difficulty: "beginner", question: "Which SQL statement is used to create a database table?", options: ["CREATE TABLE", "MAKE TABLE", "BUILD TABLE", "NEW TABLE"], correct: 0, example: "CREATE TABLE employees (\n  id INT PRIMARY KEY,\n  name VARCHAR(100)\n);", explanation: "CREATE TABLE statement is used to create a new table in a database." },
    { id: 14, difficulty: "beginner", question: "Which SQL statement is used to delete a table?", options: ["DELETE TABLE", "REMOVE TABLE", "DROP TABLE", "ERASE TABLE"], correct: 2, example: "DROP TABLE employees;", explanation: "DROP TABLE statement is used to delete an entire table including its structure and data." },
    { id: 15, difficulty: "beginner", question: "Which SQL statement is used to change data in a table?", options: ["MODIFY", "UPDATE", "CHANGE", "ALTER"], correct: 1, example: "UPDATE products\nSET price = 29.99\nWHERE product_id = 5;", explanation: "UPDATE statement modifies existing records. ALTER is used to modify table structure, not data." },
    { id: 16, difficulty: "beginner", question: "What is the correct SQL syntax for returning all columns from 'Orders'?", options: ["GET ALL FROM Orders", "SELECT ALL FROM Orders", "SELECT * FROM Orders", "RETURN * FROM Orders"], correct: 2, example: "SELECT * FROM Orders;", explanation: "SELECT * is the standard syntax to retrieve all columns." },
    { id: 17, difficulty: "beginner", question: "Which SQL clause is used to combine rows from two or more tables?", options: ["COMBINE", "JOIN", "MERGE", "CONNECT"], correct: 1, example: "SELECT orders.id, customers.name\nFROM orders\nJOIN customers ON orders.customer_id = customers.id;", explanation: "JOIN clause is used to combine rows from two or more tables based on a related column." },
    { id: 18, difficulty: "beginner", question: "What does the SQL 'AS' keyword do?", options: ["Assigns a value", "Creates an alias", "Adds a column", "Sorts data"], correct: 1, example: "SELECT name AS employee_name\nFROM employees;", explanation: "AS keyword is used to create an alias (temporary name) for a column or table." },
    { id: 19, difficulty: "beginner", question: "Which SQL function returns the largest value of the selected column?", options: ["TOP()", "MAX()", "LARGEST()", "HIGHEST()"], correct: 1, example: "SELECT MAX(salary) AS highest_salary\nFROM employees;", explanation: "MAX() function returns the maximum value in a set of values." },
    { id: 20, difficulty: "beginner", question: "Which SQL function returns the smallest value of the selected column?", options: ["MIN()", "BOTTOM()", "SMALLEST()", "LOWEST()"], correct: 0, example: "SELECT MIN(price) AS lowest_price\nFROM products;", explanation: "MIN() function returns the minimum value in a set of values." },
    { id: 21, difficulty: "beginner", question: "Which SQL function returns the sum of a numeric column?", options: ["TOTAL()", "ADD()", "SUM()", "COUNT()"], correct: 2, example: "SELECT SUM(quantity) AS total_items\nFROM order_details;", explanation: "SUM() function returns the total sum of a numeric column." },
    { id: 22, difficulty: "beginner", question: "Which SQL function returns the average value of a numeric column?", options: ["AVG()", "MEAN()", "AVERAGE()", "MEDIAN()"], correct: 0, example: "SELECT AVG(salary) AS average_salary\nFROM employees;", explanation: "AVG() function returns the average value of a numeric column." },
    { id: 23, difficulty: "beginner", question: "What does NULL represent in SQL?", options: ["Zero value", "Empty string", "Missing or unknown value", "False value"], correct: 2, example: "SELECT * FROM employees\nWHERE middle_name IS NULL;", explanation: "NULL represents a missing or unknown value. Use IS NULL to check for NULL values." },
    { id: 24, difficulty: "beginner", question: "Which operator is used to specify multiple possible values for a column?", options: ["BETWEEN", "IN", "OR", "ANY"], correct: 1, example: "SELECT * FROM employees\nWHERE department IN ('Sales', 'Marketing');", explanation: "IN operator allows you to specify multiple values in a WHERE clause." },
    { id: 25, difficulty: "beginner", question: "Which operator selects values within a given range?", options: ["WITHIN", "RANGE", "BETWEEN", "IN"], correct: 2, example: "SELECT * FROM products\nWHERE price BETWEEN 10 AND 50;", explanation: "BETWEEN operator selects values within a given range (inclusive)." },
    { id: 26, difficulty: "beginner", question: "What is the default sort order for ORDER BY clause?", options: ["Descending", "Ascending", "Random", "No order"], correct: 1, example: "SELECT * FROM employees\nORDER BY name;", explanation: "ORDER BY sorts in ascending order (ASC) by default." },
    { id: 27, difficulty: "beginner", question: "Which SQL clause is used to group rows that have the same values?", options: ["GROUP BY", "COLLECT BY", "GATHER BY", "COMBINE BY"], correct: 0, example: "SELECT department, COUNT(*)\nFROM employees\nGROUP BY department;", explanation: "GROUP BY groups rows with same values into summary rows." },
    { id: 28, difficulty: "beginner", question: "Which SQL keyword is used with aggregate functions to filter groups?", options: ["WHERE", "FILTER", "HAVING", "IF"], correct: 2, example: "SELECT department, AVG(salary)\nFROM employees\nGROUP BY department\nHAVING AVG(salary) > 50000;", explanation: "HAVING clause filters groups after GROUP BY." },
    { id: 29, difficulty: "beginner", question: "What does the '%' wildcard represent in SQL?", options: ["Exactly one character", "Zero or more characters", "One or more characters", "Any single digit"], correct: 1, example: "SELECT * FROM customers\nWHERE name LIKE 'J%';", explanation: "% wildcard represents zero or more characters." },
    { id: 30, difficulty: "beginner", question: "What does the '_' wildcard represent in SQL?", options: ["Zero or more characters", "Exactly one character", "One or more characters", "Space character"], correct: 1, example: "SELECT * FROM products\nWHERE code LIKE 'A_C';", explanation: "_ (underscore) wildcard represents exactly one character." },
    { id: 31, difficulty: "beginner", question: "Which SQL statement is used to add a new column to an existing table?", options: ["ADD COLUMN", "ALTER TABLE ADD", "MODIFY TABLE", "INSERT COLUMN"], correct: 1, example: "ALTER TABLE employees\nADD email VARCHAR(100);", explanation: "ALTER TABLE with ADD clause is used to add new columns." },
    { id: 32, difficulty: "beginner", question: "What is a PRIMARY KEY in SQL?", options: ["A key that can have duplicate values", "A unique identifier for each record", "A foreign key reference", "An optional field"], correct: 1, example: "CREATE TABLE students (\n  student_id INT PRIMARY KEY,\n  name VARCHAR(100)\n);", explanation: "PRIMARY KEY uniquely identifies each record in a table." },
    { id: 33, difficulty: "beginner", question: "What is a FOREIGN KEY in SQL?", options: ["A key from another database", "A key that links two tables together", "A duplicate primary key", "An external reference"], correct: 1, example: "CREATE TABLE orders (\n  order_id INT PRIMARY KEY,\n  customer_id INT,\n  FOREIGN KEY (customer_id) REFERENCES customers(id)\n);", explanation: "FOREIGN KEY is a field that links to the PRIMARY KEY in another table." },
    { id: 34, difficulty: "beginner", question: "Which SQL constraint ensures that a column cannot have NULL values?", options: ["UNIQUE", "NOT NULL", "CHECK", "DEFAULT"], correct: 1, example: "CREATE TABLE employees (\n  id INT PRIMARY KEY,\n  name VARCHAR(100) NOT NULL\n);", explanation: "NOT NULL constraint ensures that a column must always contain a value." },
    { id: 35, difficulty: "beginner", question: "Which SQL constraint ensures all values in a column are different?", options: ["DISTINCT", "DIFFERENT", "UNIQUE", "PRIMARY"], correct: 2, example: "CREATE TABLE users (\n  id INT PRIMARY KEY,\n  email VARCHAR(100) UNIQUE\n);", explanation: "UNIQUE constraint ensures that all values in a column are different." },
    { id: 36, difficulty: "beginner", question: "What does the LIMIT clause do in SQL?", options: ["Limits the number of columns", "Limits the number of rows returned", "Limits the query execution time", "Limits the table size"], correct: 1, example: "SELECT * FROM employees\nLIMIT 10;", explanation: "LIMIT clause specifies the maximum number of rows to return." },
    { id: 37, difficulty: "beginner", question: "Which SQL operator is used to combine multiple conditions with AND logic?", options: ["AND", "&&", "BOTH", "ALSO"], correct: 0, example: "SELECT * FROM employees\nWHERE salary > 50000 AND department = 'IT';", explanation: "AND operator combines multiple conditions. All conditions must be true." },
    { id: 38, difficulty: "beginner", question: "Which SQL operator is used to combine multiple conditions with OR logic?", options: ["||", "OR", "EITHER", "ANY"], correct: 1, example: "SELECT * FROM employees\nWHERE department = 'Sales' OR department = 'Marketing';", explanation: "OR operator combines multiple conditions. At least one condition must be true." },
    { id: 39, difficulty: "beginner", question: "What does the NOT operator do in SQL?", options: ["Negates a condition", "Removes duplicates", "Sorts in reverse", "Deletes records"], correct: 0, example: "SELECT * FROM employees\nWHERE NOT department = 'HR';", explanation: "NOT operator reverses the result of a condition." },
    { id: 40, difficulty: "beginner", question: "Which SQL function converts a value to uppercase?", options: ["UPPER()", "UCASE()", "UPPERCASE()", "CAPS()"], correct: 0, example: "SELECT UPPER(name) AS uppercase_name\nFROM employees;", explanation: "UPPER() function converts a string to uppercase letters." },

    // INTERMEDIATE LEVEL (41-70)
    { id: 41, difficulty: "intermediate", question: "What is the difference between INNER JOIN and LEFT JOIN?", options: ["No difference", "INNER JOIN returns all rows from left table", "INNER JOIN returns matching rows only, LEFT JOIN returns all rows from left table", "LEFT JOIN is faster"], correct: 2, example: "-- INNER JOIN\nSELECT * FROM orders o\nINNER JOIN customers c ON o.customer_id = c.id;\n\n-- LEFT JOIN\nSELECT * FROM orders o\nLEFT JOIN customers c ON o.customer_id = c.id;", explanation: "INNER JOIN returns only matching rows. LEFT JOIN returns all rows from left table and matching rows from right table." },
    { id: 42, difficulty: "intermediate", question: "What is a subquery in SQL?", options: ["A query that runs after the main query", "A query nested inside another query", "A query that updates data", "A query with multiple tables"], correct: 1, example: "SELECT name, salary\nFROM employees\nWHERE salary > (SELECT AVG(salary) FROM employees);", explanation: "A subquery is a query nested inside another query." },
    { id: 43, difficulty: "intermediate", question: "What is the purpose of the UNION operator?", options: ["To join two tables", "To combine result sets of two or more SELECT statements", "To update multiple tables", "To create relationships"], correct: 1, example: "SELECT name FROM employees\nUNION\nSELECT name FROM contractors;", explanation: "UNION combines result sets of two or more SELECT statements, removing duplicates." },
    { id: 44, difficulty: "intermediate", question: "What is the difference between UNION and UNION ALL?", options: ["No difference", "UNION removes duplicates, UNION ALL keeps duplicates", "UNION ALL removes duplicates", "UNION is faster"], correct: 1, example: "-- UNION (removes duplicates)\nSELECT city FROM customers\nUNION\nSELECT city FROM suppliers;", explanation: "UNION removes duplicate rows. UNION ALL includes all rows, even duplicates." },
    { id: 45, difficulty: "intermediate", question: "What is an INDEX in SQL?", options: ["A table column", "A data structure that improves query performance", "A type of constraint", "A backup of data"], correct: 1, example: "CREATE INDEX idx_employee_name\nON employees(name);", explanation: "An INDEX is a data structure that speeds up data retrieval." },
    { id: 46, difficulty: "intermediate", question: "What is the purpose of the COALESCE function?", options: ["To combine strings", "To return the first non-NULL value", "To count NULL values", "To remove NULL values"], correct: 1, example: "SELECT name,\n  COALESCE(phone, email, 'No contact') AS contact\nFROM customers;", explanation: "COALESCE returns the first non-NULL value in a list." },
    { id: 47, difficulty: "intermediate", question: "What is a VIEW in SQL?", options: ["A physical table", "A virtual table based on a SELECT query", "A backup of data", "A type of index"], correct: 1, example: "CREATE VIEW high_earners AS\nSELECT name, salary\nFROM employees\nWHERE salary > 100000;", explanation: "A VIEW is a virtual table based on a SELECT query." },
    { id: 48, difficulty: "intermediate", question: "What is the CASE statement used for?", options: ["To create conditions like if-else", "To switch databases", "To change data types", "To handle errors"], correct: 0, example: "SELECT name,\n  CASE\n    WHEN salary < 50000 THEN 'Low'\n    WHEN salary < 100000 THEN 'Medium'\n    ELSE 'High'\n  END AS salary_range\nFROM employees;", explanation: "CASE statement provides if-then-else logic in SQL." },
    { id: 49, difficulty: "intermediate", question: "What is the difference between DELETE and TRUNCATE?", options: ["No difference", "DELETE can use WHERE clause, TRUNCATE removes all rows", "TRUNCATE can use WHERE clause", "DELETE is faster"], correct: 1, example: "-- DELETE (conditional)\nDELETE FROM employees WHERE department = 'Sales';\n\n-- TRUNCATE (all rows)\nTRUNCATE TABLE employees;", explanation: "DELETE can remove specific rows using WHERE. TRUNCATE removes all rows and is faster." },
    { id: 50, difficulty: "intermediate", question: "What is a SELF JOIN?", options: ["A join with itself", "A join that returns the same table twice", "A table joined with itself to compare rows", "An automatic join"], correct: 2, example: "SELECT e1.name AS employee,\n  e2.name AS manager\nFROM employees e1\nJOIN employees e2 ON e1.manager_id = e2.id;", explanation: "SELF JOIN is when a table is joined with itself." },
    { id: 51, difficulty: "intermediate", question: "What does the EXISTS operator do?", options: ["Checks if a table exists", "Checks if a subquery returns any rows", "Checks if a value exists", "Checks if a database exists"], correct: 1, example: "SELECT name FROM customers c\nWHERE EXISTS (\n  SELECT 1 FROM orders o\n  WHERE o.customer_id = c.id\n);", explanation: "EXISTS returns true if the subquery returns one or more rows." },
    { id: 52, difficulty: "intermediate", question: "What is the purpose of the HAVING clause?", options: ["To filter rows before grouping", "To filter groups after GROUP BY", "To sort results", "To join tables"], correct: 1, example: "SELECT department, COUNT(*)\nFROM employees\nGROUP BY department\nHAVING COUNT(*) > 5;", explanation: "HAVING filters groups created by GROUP BY." },
    { id: 53, difficulty: "intermediate", question: "What is a CROSS JOIN?", options: ["A join that crosses databases", "A join that returns the Cartesian product", "A join that crosses columns", "An invalid join"], correct: 1, example: "SELECT * FROM colors\nCROSS JOIN sizes;", explanation: "CROSS JOIN returns the Cartesian product - every row from first table combined with every row from second table." },
    { id: 54, difficulty: "intermediate", question: "What is the difference between WHERE and HAVING?", options: ["No difference", "WHERE filters rows, HAVING filters groups", "HAVING filters rows", "WHERE is faster"], correct: 1, example: "SELECT department, AVG(salary)\nFROM employees\nWHERE salary > 30000\nGROUP BY department\nHAVING AVG(salary) > 50000;", explanation: "WHERE filters individual rows before grouping. HAVING filters groups after GROUP BY." },
    { id: 55, difficulty: "intermediate", question: "What is the purpose of the DISTINCT keyword?", options: ["To sort results", "To remove duplicate rows", "To count rows", "To filter rows"], correct: 1, example: "SELECT DISTINCT department\nFROM employees;", explanation: "DISTINCT removes duplicate rows from the result set." },
    { id: 56, difficulty: "intermediate", question: "What is a COMPOSITE KEY?", options: ["A key made of multiple columns", "A key with complex data type", "A foreign key", "An encrypted key"], correct: 0, example: "CREATE TABLE enrollment (\n  student_id INT,\n  course_id INT,\n  PRIMARY KEY (student_id, course_id)\n);", explanation: "A COMPOSITE KEY is a primary key consisting of multiple columns." },
    { id: 57, difficulty: "intermediate", question: "What is the purpose of the GROUP_CONCAT function?", options: ["To group rows", "To concatenate values from multiple rows into a single string", "To count groups", "To split strings"], correct: 1, example: "SELECT department,\n  GROUP_CONCAT(name) AS employees\nFROM employees\nGROUP BY department;", explanation: "GROUP_CONCAT concatenates values from multiple rows into a single string." },
    { id: 58, difficulty: "intermediate", question: "What is the difference between CHAR and VARCHAR?", options: ["No difference", "CHAR is fixed-length, VARCHAR is variable-length", "VARCHAR is fixed-length", "CHAR is faster"], correct: 1, example: "CREATE TABLE users (\n  code CHAR(5),\n  name VARCHAR(100)\n);", explanation: "CHAR stores fixed-length strings. VARCHAR stores variable-length strings." },
    { id: 59, difficulty: "intermediate", question: "What is a TRANSACTION in SQL?", options: ["A single SQL statement", "A sequence of operations performed as a single unit", "A type of join", "A database backup"], correct: 1, example: "BEGIN TRANSACTION;\nUPDATE accounts SET balance = balance - 100 WHERE id = 1;\nUPDATE accounts SET balance = balance + 100 WHERE id = 2;\nCOMMIT;", explanation: "A TRANSACTION is a sequence of operations treated as a single unit." },
    { id: 60, difficulty: "intermediate", question: "What does COMMIT do in SQL?", options: ["Saves changes permanently", "Cancels changes", "Creates a backup", "Closes the database"], correct: 0, example: "BEGIN TRANSACTION;\nINSERT INTO orders VALUES (1, 'Product A');\nCOMMIT;", explanation: "COMMIT saves all changes made in the current transaction permanently." },
    { id: 61, difficulty: "intermediate", question: "What does ROLLBACK do in SQL?", options: ["Saves changes", "Undoes changes made in the current transaction", "Creates a backup", "Restores from backup"], correct: 1, example: "BEGIN TRANSACTION;\nDELETE FROM orders WHERE id = 1;\nROLLBACK;", explanation: "ROLLBACK undoes all changes made in the current transaction." },
    { id: 62, difficulty: "intermediate", question: "What is the purpose of the OFFSET clause?", options: ["To skip a specified number of rows", "To limit results", "To sort results", "To filter results"], correct: 0, example: "SELECT * FROM employees\nLIMIT 10 OFFSET 20;", explanation: "OFFSET skips a specified number of rows before returning results." },
    { id: 63, difficulty: "intermediate", question: "What is a STORED PROCEDURE?", options: ["A saved query", "A precompiled collection of SQL statements", "A backup procedure", "A type of index"], correct: 1, example: "CREATE PROCEDURE GetEmployeesByDept(IN dept VARCHAR(50))\nBEGIN\n  SELECT * FROM employees WHERE department = dept;\nEND;", explanation: "A STORED PROCEDURE is a prepared SQL code that can be saved and reused." },
    { id: 64, difficulty: "intermediate", question: "What is the difference between RANK() and DENSE_RANK()?", options: ["No difference", "RANK() skips numbers after ties, DENSE_RANK() doesn't", "DENSE_RANK() skips numbers", "RANK() is faster"], correct: 1, example: "SELECT name, salary,\n  RANK() OVER (ORDER BY salary DESC) as rank,\n  DENSE_RANK() OVER (ORDER BY salary DESC) as dense_rank\nFROM employees;", explanation: "RANK() leaves gaps in ranking after ties (1,2,2,4). DENSE_RANK() doesn't leave gaps (1,2,2,3)." },
    { id: 65, difficulty: "intermediate", question: "What is the ROW_NUMBER() function?", options: ["Counts total rows", "Assigns a unique sequential number to each row", "Returns the row ID", "Counts duplicate rows"], correct: 1, example: "SELECT name, salary,\n  ROW_NUMBER() OVER (ORDER BY salary DESC) as row_num\nFROM employees;", explanation: "ROW_NUMBER() assigns a unique sequential integer to each row." },
    { id: 66, difficulty: "intermediate", question: "What is a TRIGGER in SQL?", options: ["A manual command", "An automatic action that executes when a specified event occurs", "A type of constraint", "A backup mechanism"], correct: 1, example: "CREATE TRIGGER update_timestamp\nBEFORE UPDATE ON employees\nFOR EACH ROW\nSET NEW.updated_at = NOW();", explanation: "A TRIGGER is a stored procedure that automatically executes when a specified event occurs." },
    { id: 67, difficulty: "intermediate", question: "What is the purpose of the PARTITION BY clause?", options: ["To split a table", "To divide result set into partitions for window functions", "To create table partitions", "To filter results"], correct: 1, example: "SELECT name, department, salary,\n  AVG(salary) OVER (PARTITION BY department) as dept_avg\nFROM employees;", explanation: "PARTITION BY divides the result set into partitions for window functions." },
    { id: 68, difficulty: "intermediate", question: "What is the difference between UNION and JOIN?", options: ["No difference", "UNION combines rows vertically, JOIN combines columns horizontally", "JOIN combines rows vertically", "UNION is faster"], correct: 1, example: "-- UNION (vertical)\nSELECT name FROM employees\nUNION\nSELECT name FROM contractors;\n\n-- JOIN (horizontal)\nSELECT e.name, d.dept_name\nFROM employees e\nJOIN departments d ON e.dept_id = d.id;", explanation: "UNION stacks result sets vertically. JOIN combines tables horizontally." },
    { id: 69, difficulty: "intermediate", question: "What is the NULLIF function used for?", options: ["To check for NULL values", "To return NULL if two expressions are equal", "To remove NULL values", "To count NULL values"], correct: 1, example: "SELECT name,\n  NULLIF(phone, '') AS phone\nFROM customers;", explanation: "NULLIF returns NULL if two expressions are equal, otherwise returns the first expression." },
    { id: 70, difficulty: "intermediate", question: "What is a CURSOR in SQL?", options: ["A mouse pointer", "A database object to retrieve and manipulate rows one at a time", "A type of index", "A backup tool"], correct: 1, example: "DECLARE emp_cursor CURSOR FOR\nSELECT name, salary FROM employees;\n\nOPEN emp_cursor;\nFETCH NEXT FROM emp_cursor;", explanation: "A CURSOR allows row-by-row processing of query results." },

    // ADVANCED LEVEL (71-100)
    { id: 71, difficulty: "advanced", question: "What is database normalization?", options: ["Making database faster", "Organizing data to reduce redundancy and improve integrity", "Creating backups", "Encrypting data"], correct: 1, example: "-- Unnormalized\nOrders: order_id, customer_name, customer_email\n\n-- Normalized\nCustomers: customer_id, name, email\nOrders: order_id, customer_id", explanation: "Normalization organizes data to minimize redundancy and dependency." },
    { id: 72, difficulty: "advanced", question: "What is the First Normal Form (1NF)?", options: ["All columns must be unique", "Each column contains atomic values", "No duplicate rows", "All columns must have primary keys"], correct: 1, example: "-- Not 1NF\nstudent_id | courses\n1 | Math, Science\n\n-- 1NF\nstudent_id | course\n1 | Math\n1 | Science", explanation: "1NF requires that each column contains atomic (single, indivisible) values." },
    { id: 73, difficulty: "advanced", question: "What is the Second Normal Form (2NF)?", options: ["Must be in 1NF and all non-key columns fully depend on primary key", "Must have two primary keys", "Must have foreign keys", "Must be in 3NF first"], correct: 0, example: "-- Not 2NF\norder_id | product_id | product_name | quantity\n\n-- 2NF\nOrders: order_id, product_id, quantity\nProducts: product_id, product_name", explanation: "2NF requires 1NF plus all non-key attributes must depend on the entire primary key." },
    { id: 74, difficulty: "advanced", question: "What is the Third Normal Form (3NF)?", options: ["Must be in 2NF and no transitive dependencies", "Must have three tables", "Must have three primary keys", "Must be in 4NF first"], correct: 0, example: "-- Not 3NF\nemp_id | dept_id | dept_name\n\n-- 3NF\nEmployees: emp_id, dept_id\nDepartments: dept_id, dept_name", explanation: "3NF requires 2NF plus no transitive dependencies." },
    { id: 75, difficulty: "advanced", question: "What is a WINDOW FUNCTION in SQL?", options: ["A function that opens windows", "A function that performs calculations across a set of rows", "A function that filters data", "A function that creates views"], correct: 1, example: "SELECT name, salary,\n  AVG(salary) OVER (PARTITION BY department) as dept_avg,\n  RANK() OVER (ORDER BY salary DESC) as rank\nFROM employees;", explanation: "Window functions perform calculations across a set of rows related to the current row." },
    { id: 76, difficulty: "advanced", question: "What is the difference between OLTP and OLAP?", options: ["No difference", "OLTP for transactions, OLAP for analytics", "OLAP for transactions", "OLTP is newer"], correct: 1, example: "-- OLTP: Fast transactions\nINSERT INTO orders VALUES (...);\n\n-- OLAP: Complex analytics\nSELECT year, region, SUM(sales)\nFROM sales_data\nGROUP BY year, region;", explanation: "OLTP handles day-to-day transactions. OLAP handles complex queries for analysis." },
    { id: 77, difficulty: "advanced", question: "What is a MATERIALIZED VIEW?", options: ["A regular view", "A view that stores query results physically", "A temporary view", "A view that cannot be updated"], correct: 1, example: "CREATE MATERIALIZED VIEW sales_summary AS\nSELECT product_id, SUM(quantity)\nFROM orders\nGROUP BY product_id;\n\nREFRESH MATERIALIZED VIEW sales_summary;", explanation: "A MATERIALIZED VIEW stores the query result physically for better performance." },
    { id: 78, difficulty: "advanced", question: "What is the purpose of EXPLAIN in SQL?", options: ["To add comments", "To show query execution plan", "To explain errors", "To document queries"], correct: 1, example: "EXPLAIN SELECT * FROM employees\nWHERE department = 'IT';", explanation: "EXPLAIN shows how the database will execute a query, useful for optimization." },
    { id: 79, difficulty: "advanced", question: "What is a CLUSTERED INDEX?", options: ["An index on multiple columns", "An index that determines the physical order of data", "A temporary index", "An index on foreign keys"], correct: 1, example: "CREATE CLUSTERED INDEX idx_emp_id\nON employees(emp_id);", explanation: "A CLUSTERED INDEX determines the physical order of data in a table. A table can have only one clustered index." },
    { id: 80, difficulty: "advanced", question: "What is a NON-CLUSTERED INDEX?", options: ["An index without clustering", "An index that doesn't affect physical data order", "An invalid index", "An index on text columns"], correct: 1, example: "CREATE NONCLUSTERED INDEX idx_emp_name\nON employees(name);", explanation: "A NON-CLUSTERED INDEX creates a separate structure that points to the data. A table can have multiple non-clustered indexes." },
    { id: 81, difficulty: "advanced", question: "What is the purpose of the WITH clause (CTE)?", options: ["To create temporary tables", "To define a temporary result set", "To join tables", "To create views"], correct: 1, example: "WITH high_earners AS (\n  SELECT * FROM employees\n  WHERE salary > 100000\n)\nSELECT * FROM high_earners\nWHERE department = 'IT';", explanation: "WITH clause (Common Table Expression) defines a temporary result set that can be referenced within a SELECT, INSERT, UPDATE, or DELETE statement." },
    { id: 82, difficulty: "advanced", question: "What is a RECURSIVE CTE?", options: ["A CTE that calls itself", "A CTE with multiple queries", "A CTE that loops forever", "An invalid CTE"], correct: 0, example: "WITH RECURSIVE emp_hierarchy AS (\n  SELECT emp_id, name, manager_id, 1 as level\n  FROM employees WHERE manager_id IS NULL\n  UNION ALL\n  SELECT e.emp_id, e.name, e.manager_id, eh.level + 1\n  FROM employees e\n  JOIN emp_hierarchy eh ON e.manager_id = eh.emp_id\n)\nSELECT * FROM emp_hierarchy;", explanation: "A RECURSIVE CTE references itself to process hierarchical data like organizational structures." },
    { id: 83, difficulty: "advanced", question: "What is the ACID property in databases?", options: ["A type of query", "Atomicity, Consistency, Isolation, Durability", "A backup method", "A security feature"], correct: 1, example: "BEGIN TRANSACTION;\n-- Atomicity: All or nothing\n-- Consistency: Valid state\n-- Isolation: Concurrent transactions\n-- Durability: Permanent changes\nCOMMIT;", explanation: "ACID ensures reliable transaction processing: Atomicity (all or nothing), Consistency (valid state), Isolation (concurrent safety), Durability (permanent changes)." },
    { id: 84, difficulty: "advanced", question: "What is a DEADLOCK in SQL?", options: ["A locked table", "Two or more transactions waiting for each other", "A slow query", "A security lock"], correct: 1, example: "-- Transaction 1\nBEGIN; UPDATE accounts SET balance = 100 WHERE id = 1;\nUPDATE accounts SET balance = 200 WHERE id = 2;\n\n-- Transaction 2\nBEGIN; UPDATE accounts SET balance = 200 WHERE id = 2;\nUPDATE accounts SET balance = 100 WHERE id = 1;", explanation: "A DEADLOCK occurs when two or more transactions are waiting for each other to release locks, creating a circular dependency." },
    { id: 85, difficulty: "advanced", question: "What is the purpose of ISOLATION LEVELS?", options: ["To isolate databases", "To control transaction visibility and locking behavior", "To create backups", "To secure data"], correct: 1, example: "SET TRANSACTION ISOLATION LEVEL READ COMMITTED;\n-- Levels: READ UNCOMMITTED, READ COMMITTED,\n-- REPEATABLE READ, SERIALIZABLE", explanation: "ISOLATION LEVELS control how transaction integrity is visible to other users and systems, balancing consistency and performance." },
    { id: 86, difficulty: "advanced", question: "What is a FULL OUTER JOIN?", options: ["A complete join", "Returns all rows from both tables with matches where available", "Returns only matching rows", "An invalid join"], correct: 1, example: "SELECT *\nFROM customers c\nFULL OUTER JOIN orders o ON c.id = o.customer_id;", explanation: "FULL OUTER JOIN returns all rows from both tables, with NULL for non-matching rows." },
    { id: 87, difficulty: "advanced", question: "What is the difference between TRUNCATE and DELETE?", options: ["No difference", "TRUNCATE is faster and cannot be rolled back in most databases", "DELETE is faster", "TRUNCATE can use WHERE"], correct: 1, example: "-- DELETE: Can be rolled back, slower\nDELETE FROM employees;\n\n-- TRUNCATE: Faster, resets identity\nTRUNCATE TABLE employees;", explanation: "TRUNCATE is faster, resets identity columns, and cannot use WHERE clause. DELETE is slower but more flexible." },
    { id: 88, difficulty: "advanced", question: "What is a PIVOT operation?", options: ["To rotate a table", "To transform rows into columns", "To sort data", "To filter data"], correct: 1, example: "SELECT *\nFROM (\n  SELECT department, month, sales\n  FROM sales_data\n) AS source\nPIVOT (\n  SUM(sales)\n  FOR month IN ([Jan], [Feb], [Mar])\n) AS pivot_table;", explanation: "PIVOT transforms rows into columns, useful for creating cross-tabulation reports." },
    { id: 89, difficulty: "advanced", question: "What is an UNPIVOT operation?", options: ["To reverse a pivot", "To transform columns into rows", "To delete columns", "To merge tables"], correct: 1, example: "SELECT department, month, sales\nFROM sales_pivot\nUNPIVOT (\n  sales FOR month IN ([Jan], [Feb], [Mar])\n) AS unpivot_table;", explanation: "UNPIVOT transforms columns into rows, the reverse of PIVOT operation." },
    { id: 90, difficulty: "advanced", question: "What is the purpose of the MERGE statement?", options: ["To merge databases", "To perform INSERT, UPDATE, or DELETE in a single statement", "To combine tables", "To join tables"], correct: 1, example: "MERGE INTO target_table t\nUSING source_table s ON t.id = s.id\nWHEN MATCHED THEN UPDATE SET t.value = s.value\nWHEN NOT MATCHED THEN INSERT VALUES (s.id, s.value);", explanation: "MERGE performs INSERT, UPDATE, or DELETE operations in a single statement based on conditions." },
    { id: 91, difficulty: "advanced", question: "What is a BITMAP INDEX?", options: ["An image index", "An index using bitmaps for low-cardinality columns", "A graphical index", "An invalid index"], correct: 1, example: "CREATE BITMAP INDEX idx_gender\nON employees(gender);", explanation: "BITMAP INDEX uses bitmaps and is efficient for columns with low cardinality (few distinct values)." },
    { id: 92, difficulty: "advanced", question: "What is QUERY OPTIMIZATION?", options: ["Making queries shorter", "Improving query performance through various techniques", "Removing queries", "Encrypting queries"], correct: 1, example: "-- Use indexes\nCREATE INDEX idx_dept ON employees(department);\n\n-- Avoid SELECT *\nSELECT name, salary FROM employees;\n\n-- Use WHERE instead of HAVING when possible", explanation: "QUERY OPTIMIZATION involves techniques like using indexes, avoiding SELECT *, proper JOIN usage, and analyzing execution plans." },
    { id: 93, difficulty: "advanced", question: "What is a COVERING INDEX?", options: ["An index that covers a table", "An index that includes all columns needed for a query", "A full-text index", "A temporary index"], correct: 1, example: "CREATE INDEX idx_covering\nON employees(department, name, salary);", explanation: "A COVERING INDEX includes all columns needed for a query, allowing the database to satisfy the query using only the index." },
    { id: 94, difficulty: "advanced", question: "What is DATABASE SHARDING?", options: ["Breaking a database", "Partitioning data across multiple databases", "Backing up data", "Encrypting data"], correct: 1, example: "-- Horizontal sharding\nDB1: users WHERE id % 3 = 0\nDB2: users WHERE id % 3 = 1\nDB3: users WHERE id % 3 = 2", explanation: "SHARDING is a database architecture pattern that partitions data across multiple databases to improve scalability." },
    { id: 95, difficulty: "advanced", question: "What is DATABASE REPLICATION?", options: ["Copying a database", "Maintaining multiple copies of data across different servers", "Backing up data", "Deleting data"], correct: 1, example: "-- Master-Slave Replication\nMaster DB: Handles writes\nSlave DB1: Read replica\nSlave DB2: Read replica", explanation: "REPLICATION maintains multiple copies of data across different servers for high availability and load distribution." },
    { id: 96, difficulty: "advanced", question: "What is a SAVEPOINT in transactions?", options: ["A backup point", "A point within a transaction to which you can rollback", "A commit point", "A checkpoint"], correct: 1, example: "BEGIN TRANSACTION;\nINSERT INTO orders VALUES (1, 'Product A');\nSAVEPOINT sp1;\nINSERT INTO orders VALUES (2, 'Product B');\nROLLBACK TO sp1;\nCOMMIT;", explanation: "SAVEPOINT creates a point within a transaction to which you can rollback without rolling back the entire transaction." },
    { id: 97, difficulty: "advanced", question: "What is the N+1 query problem?", options: ["A syntax error", "Executing N additional queries for N results", "A performance issue", "A security vulnerability"], correct: 2, example: "-- N+1 Problem\nSELECT * FROM orders; -- 1 query\nFOR each order:\n  SELECT * FROM customers WHERE id = order.customer_id; -- N queries\n\n-- Solution: Use JOIN\nSELECT o.*, c.*\nFROM orders o\nJOIN customers c ON o.customer_id = c.id;", explanation: "N+1 problem occurs when you execute one query to get N results, then N additional queries to get related data. Use JOINs to solve it." },
    { id: 98, difficulty: "advanced", question: "What is a FUNCTION-BASED INDEX?", options: ["An index on functions", "An index on the result of a function applied to columns", "A temporary index", "An invalid index"], correct: 1, example: "CREATE INDEX idx_upper_name\nON employees(UPPER(name));", explanation: "FUNCTION-BASED INDEX creates an index on the result of a function, useful for queries that use functions in WHERE clauses." },
    { id: 99, difficulty: "advanced", question: "What is the purpose of ANALYZE/STATISTICS in SQL?", options: ["To analyze errors", "To collect statistics about tables for query optimization", "To analyze users", "To create reports"], correct: 1, example: "ANALYZE TABLE employees;\n-- or\nUPDATE STATISTICS employees;", explanation: "ANALYZE/STATISTICS collects information about data distribution in tables, helping the query optimizer make better decisions." },
    { id: 100, difficulty: "advanced", question: "What is a TEMPORAL TABLE?", options: ["A temporary table", "A table that tracks historical changes", "A time-based table", "An invalid table"], correct: 1, example: "CREATE TABLE employees (\n  id INT PRIMARY KEY,\n  name VARCHAR(100),\n  salary DECIMAL(10,2),\n  valid_from DATETIME,\n  valid_to DATETIME\n);", explanation: "TEMPORAL TABLE (or System-Versioned Table) automatically tracks historical changes, allowing you to query data as it existed at any point in time." }
];

// Made with Bob
